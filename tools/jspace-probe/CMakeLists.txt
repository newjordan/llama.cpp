set(TARGET llama-jspace-probe)
add_executable(${TARGET} jspace-probe.cpp fibonacci-pool.h)
target_link_libraries(${TARGET} PRIVATE llama-common llama ${CMAKE_THREAD_LIBS_INIT})
target_compile_features(${TARGET} PRIVATE cxx_std_17)

set(SENSOR_TARGET llama-jspace-sensor-extract)
add_executable(${SENSOR_TARGET} jspace-sensor-extract.cpp)
target_link_libraries(${SENSOR_TARGET} PRIVATE llama-common llama ${CMAKE_THREAD_LIBS_INIT})
target_compile_features(${SENSOR_TARGET} PRIVATE cxx_std_17)

if(LLAMA_TOOLS_INSTALL)
    install(TARGETS ${TARGET} ${SENSOR_TARGET} RUNTIME)
endif()

if(LLAMA_BUILD_TESTS)
    add_test(NAME test-jspace-probe-self-test COMMAND $<TARGET_FILE:${TARGET}> --self-test)
    set_tests_properties(test-jspace-probe-self-test PROPERTIES PASS_REGULAR_EXPRESSION "\"ok\": true")
    add_test(NAME test-jspace-sensor-extract-self-test COMMAND $<TARGET_FILE:${SENSOR_TARGET}> --self-test)
    set_tests_properties(test-jspace-sensor-extract-self-test PROPERTIES PASS_REGULAR_EXPRESSION "\"ok\":true")
endif()
